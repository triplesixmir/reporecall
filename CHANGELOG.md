# Changelog

## Unreleased

- Bootstrapped the pnpm TypeScript monorepo with linting, formatting, Vitest, Playwright, and cross-platform CI.
- Added versioned memory records with Zod validation, YAML frontmatter, Markdown serialization, atomic writes, migrations, relations, tags, and malformed-file diagnostics.
- Added a rebuildable SQLite/FTS5 index and deterministic context builder with metadata filters, relation data, ranking, and token budgets.
- Added configurable paths, privacy-aware initialization, secret scanning/redaction, inbox workflows, checkpointing, and diagnostics.
- Added local stdio MCP tools and a Codex adapter with managed `AGENTS.md` and hooks blocks.
- Added optional processor contracts for agent-native, Ollama, OpenRouter, and OpenAI-compatible providers.
- Added the loopback Hono API, filesystem watcher, production web build, responsive workbench, Inbox, and relation graph.
- Added bilingual user, architecture, integration, privacy, release, contribution, and security documentation.

This release remains local-first and intentionally excludes cloud accounts, telemetry, embeddings, vector search, mobile clients, and team collaboration.
