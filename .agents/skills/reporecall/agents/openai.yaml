interface:
  display_name: "Reporecall"
  short_description: "Repo-specific patterns and workflows for reporecall"
  default_prompt: "Use the reporecall repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true