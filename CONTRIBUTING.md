# Contributing to RepoRecall

Thank you for helping improve RepoRecall. Keep changes focused, add tests before production code, and preserve the file-first architecture: canonical memory files are durable; SQLite is rebuildable cache.

## Development

```text
pnpm install
pnpm check
```

Use conventional commit messages and include a test or an explicit reason why a change is configuration-only.
